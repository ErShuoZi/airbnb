import React, { memo } from "react";
import { HeaderContentWrapper } from "./style";

const HeaderCenter = memo(() => {
  return <HeaderContentWrapper>HeaderCenter</HeaderContentWrapper>;
});

export default HeaderCenter;
